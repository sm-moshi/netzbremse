when:
  - event: pull_request
  - event: push
    branch: main

steps:
  - name: go-test
    image: golang:1.26.1-bookworm@sha256:c7a82e9e2df2fea5d8cb62a16aa6f796d2b2ed81ccad4ddd2bc9f0d22936c3f2
    commands:
      - go test ./...

  - name: browser-script-check
    image: dhi.io/node:24.14.0-debian13-dev@sha256:59eccbcc0d930d6ce9fa45f02c074daa6a36e93054421bcfa7d6857b5c882593
    commands:
      - node --check scripts/netzbremse-browser.mjs

  - name: release
    image: dhi.io/node:24.14.0-debian13-dev@sha256:59eccbcc0d930d6ce9fa45f02c074daa6a36e93054421bcfa7d6857b5c882593
    when:
      - event: push
        branch: main
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
      GH_TOKEN:
        from_secret: github_token
    commands:
      - apt-get update
      - apt-get install -y --no-install-recommends git ca-certificates
      - rm -rf /var/lib/apt/lists/*
      - rm -f .git/config.lock
      - git config user.name "woodpecker[bot]"
      - git config user.email "woodpecker@m0sh1.cc"
      - git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/sm-moshi/netzbremse.git"
      - git fetch --tags origin
      - npm ci
      - node node_modules/.bin/semantic-release
